# AGENTS.md — 에이전트 진입점 / 이어서 작업하기

> 이 저장소는 **삼대오백 / MXSSIVE 운영자의 개인 지식 위키(Obsidian Vault)**입니다.
> Antigravity·Claude Code 등 **어떤 AI 에이전트로 열든 이 파일을 먼저 읽고** 시작하세요.

## 운영 규칙 (필수)
- **전체 규칙은 [CLAUDE.md](CLAUDE.md)에 있습니다. 작업 전에 반드시 읽으세요.**
- 핵심 흐름: **`raw/`**(원본 입력·읽기전용) → **`wiki/`**(누적 지식·사고) → **`outputs/`**(업무 결과물). 일별 시계열은 **`모니터링/`**.
- 위키는 단순 아카이브가 아니라 **아이디어를 끌어내는 백데이터 엔진**이다. 상시 🛰️경쟁사 레이더 + 📡트렌드 레이더를 가동하고(자율 확장), 모든 작업 끝에 **💡제안**을 붙인다.
- 사용자는 주로 `wiki/`와 `outputs/`만 본다. 정리·발전은 에이전트가 알아서 한다.
- 🔑 시크릿(네이버 API 키 `raw/뉴스루틴_네이버api-키.txt`)은 본문에 절대 쓰지 않는다. `.gitignore`로 깃 제외됨.

## 명령
- `/뉴스정리` — `raw/`의 수집 데이터를 `모니터링/`·`wiki/`로 정리 (+경쟁사·트렌드 자율 확장)
- `/결과물` — 위키로 사고해 `outputs/`에 업무 산출물(브리핑·기획안·비교표·카피) 생성
- "건강검진" — 위키 전체 진단(끊긴 링크·고아 페이지·오래된 정보 등)

## 백업
- GitHub(비공개): https://github.com/threepeatmkt-star/samdaeobaek-wiki
- 변경 후: `git add -A && git commit -m "..." && git push`

---

## 🔖 이어서 작업하기 — 마지막 세션 (2026-06-17)

### 오늘 한 일
- 06-17 `/뉴스정리`: 시장동향(웰니스·헬시플레저·혈당·간편식·6조 박스권) 갱신. 일별 페이지는 06-16 기처리분이라 스킵.
- GitHub 비공개 백업 연결 (`samdaeobaek-wiki`). API 키 미포함 확인.
- 위키를 **"아이디어 백데이터 엔진"**으로 업그레이드 + **자율 레이더 2종**(승인 없이 알아서 추가·발전).
- 추적 경쟁사 **5사 → 8사** (일화(팅글)·하이뮨·셀렉스 자율 추가, `scripts/config.json` 등록).
- **`outputs/` 신설** + 첫 결과물 [outputs/2026-06-시장경쟁-브리핑.md](outputs/2026-06-시장경쟁-브리핑.md).
- `/결과물` 명령 신설. (참고: 유튜브 영상 RWDBO_KNPlc의 raw→wiki→outputs 구조를 적용)

### ⏳ 다음에 할 것 (pending)
1. **git 백업** — 마지막 변경분이 아직 push 안 됐을 수 있음:
   `git add -A && git commit -m "outputs 신설 등" && git push`
2. **잘못 생긴 저장소 삭제**: `gh repo delete threepeatmkt-star/0617----- --yes`
3. **바로 뽑을 수 있는 결과물**: 일화 팅글 벤치마크 비교표 / WELLNESS 신제품 기획안 초안.
4. 다음 평일 10:00 자동 수집분(`scripts/fetch-news.ps1`)이 `raw/`에 들어오면 `/뉴스정리`.

### 참고 (이 세션 특이사항)
- Anthropic 명령 안전성 분류기가 간헐적으로 다운 → `git`/`gh` 실행이 막혀, 일부 git 작업은 사용자가 터미널에서 직접 실행했다. 정상 복구되면 에이전트가 직접 실행 가능.
